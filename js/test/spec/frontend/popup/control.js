define(
  ['frontend/models/popup/control'],
  function(ControlModel) {
    xdescribe("ControlModel", function() {
    });

    xdescribe("ControlModel.play", function() {
      var controlModel = null;
      beforeEach(function() {
        controlModel = new ControlModel();
      });
    });
  }
);
