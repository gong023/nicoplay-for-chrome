define(
  ['background/models/audio'],
  function(AudioModel) {
    xdescribe("AudioModel", function() {

      it("should be true", function() {
        expect(true).toBe(true);
      });
    });
  }
);
